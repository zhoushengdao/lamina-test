name: Test Lamina

on:
  schedule:
    - cron: "30 */8 * * *"
  workflow_dispatch:

permissions:
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: cargo test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
